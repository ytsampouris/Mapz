const mapStatus = "display";
const drawType = "Point";
const measureType = "LineString";
const measureOutput = 0;
const measureUnit = "meters";
const featureInfo = {};
const featureInfoTable = "";

export default function () {
  return {
    mapStatus,
    drawType,
    measureType,
    measureOutput,
    measureUnit,
    featureInfo,
    featureInfoTable
  }
}
